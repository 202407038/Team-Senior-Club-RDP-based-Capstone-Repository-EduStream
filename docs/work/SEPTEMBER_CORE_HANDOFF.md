# 9월 1번 공통 코어 인수인계

2026-09-20 후속: #48 신규 협업 상태/메시지·개발 대역은 [최종 단계 선행 인계](./FINAL_CORE_FILE_HANDOFF.md)를 따릅니다. 아래는 기존 9월 선행 이력이며, 보안/실제 소비 연동 대기는 후속 문서에서 구분합니다.

## 주차별 결과

| 주차 | 선행 구현/검증 | 완료 범위 |
|---|---|---|
| 1주차 | 저장 권한·긴 경로·폴더·IO 오류를 ErrorCodes 및 FeatureErrorCatalog에 등록, 응답 변환 테스트 | 공통 파일 오류 계약 |
| 2주차 | RdpConnectionStatus와 실패 원인, 재접속 연결 ID·종료 전이 테스트 | 공급자 독립 상태 계약 |
| 3주차 | 한글 채팅/시스템 메시지 직렬화와 RDP·파일 상태 독립성 테스트 | 모델/직렬화 검증 |
| 4주차 | 공통 테스트 헬퍼, 학생별 종료·반복 재접속 회귀 | 순수 계약 회귀 |
| 5주차 | 아래 연동/시연 설명과 증빙 기준 | 인수인계 준비 |

선행 PR #31: 당시 전체 126/126 통과, main 반영 완료. 2026-09-15 기준 #33 WDS 계약 및 #36~#41 실제 조립/보완 반영 완료. 현재 일반 전체 186 통과 / 실제 WDS 선택 실행 3항목 건너뜀. 실제 WDS 포함 선택 검증은 11/11 통과.

## 2·3·5번 연동 계약

1. 강의 참가가 승인된 뒤 Create(sessionId, participantId)로 상태를 만듭니다. 이 모델 자체는 참가 권한을 검증하지 않습니다.
2. 연결 시도마다 새 Guid를 생성하고 BeginConnect를 호출합니다.
3. 실제 RDP 어댑터의 연결 성공 콜백에서 Connected(connectionId)를 호출합니다. 미리보기 창 생성이나 PNG 수신은 성공 근거가 아닙니다.
4. 실제 오류를 RdpFailureReason으로 변환하여 Fail을 호출합니다. NetworkInterrupted/HostUnavailable만 자동 재시도 가능한 것으로 분류합니다.
5. 재접속은 새 연결 ID로 시작합니다. 이전 연결 콜백이 들어오면 현재 상태를 바꾸지 말고 진단 로그에 남깁니다.
6. 세션 종료/참가자 이탈 시 Close를 호출합니다. Closed 상태를 재사용하지 않고 새 세션/참가 승인으로 새 상태를 만듭니다.
7. 모델은 불변입니다. 반환값을 보관해야 하며 어댑터에서 이벤트 적용을 직렬화해야 합니다. 참가자별 상태를 별도로 보관합니다.

RDP 자격 증명, 초대 토큰, 화면 본문은 이 모델에 넣지 않습니다. RDP 스트림을 기존 ScreenPacket(PNG)에 포장하지 않습니다. 기존 PacketType/ProtocolVersion은 변경하지 않았습니다. 이후 #33으로 WDS 공급자와 PacketType 9~11 초대/요청/폐기 형식을 확정했고 #36~#39로 라우팅/소비/교수자 진입점을 연결했습니다. 기존 1~8 값은 유지합니다.

## 채팅 및 파일 계약

- 사용자/시스템 메시지 생성에는 PacketFactory를 사용합니다. 길이는 글자 수가 아니라 UTF-8 바이트 수입니다.
- IsSystemMessage는 신뢰할 수 있는 서버가 결정해야 합니다. 클라이언트가 보낸 플래그만으로 시스템 메시지 권한을 부여하지 않습니다.
- 저장 오류 문자열은 기존 FileReceiver와 동일하므로 기존 수신 경로와 호환됩니다.
- FeatureOperationResult의 파일/채팅 상태와 RdpConnectionStatus를 구분해서 관리합니다.

## 5주차 시연 설명 및 기록

설명: “강의 세션 ID와 학생 ID로 연결을 구분하고, 재접속 시 새 연결 ID를 발급하여 이전 연결 이벤트가 현재 화면 상태를 바꾸지 않도록 처리.”

실제 RDP 어댑터 연동 후 시간·sessionId·participantId·connectionId·이전/현재 상태·실패 원인을 기록합니다. 비밀번호/초대 토큰은 기록하지 않습니다.

## 아직 완료되지 않은 항목

- 공급자/형식 결정과 소비 코드 연결은 완료했습니다. 이후 계약 변경이 필요한 실제 장애는 재현 근거와 함께 검토합니다.
- 실제 다중 PC의 학생 2명 RDP 연결/재접속/종료 증빙.
- 현 PC 실제 WDS+파일+채팅 자동 검증은 완료. 다중 PC 수동 병행·15분 실행 및 전체 시연 2회는 미완료.
- 모델 테스트를 RDP 네트워크/화면 품질 테스트로 표현하지 않습니다.

현 단계의 완료 범위/실행 순서는 [통합 검증 기록](./SEPTEMBER_RDP_INTEGRATION.md)과 [실행 가이드](./RUN_GUIDE.md)를 참조합니다.
