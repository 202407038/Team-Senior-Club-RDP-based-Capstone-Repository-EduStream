# 현재 상태와 로드맵

이 문서는 작업자가 현재 구현 상태와 이번 주 목표를 빠르게 확인하기 위한 공용 작업 문서입니다.

기준 자료:

- [졸작 중간발표 자료](../reference/presentations/졸작중간발표.pptx)

## 운영 규칙

- 월간 로드맵은 현재 달과 다음 달까지만 유지합니다.
- 주차별 계획은 일차별로 나누지 않고, 팀 목표치와 담당별 목표치만 관리합니다.
- 완료된 항목은 문장 끝에 `----- %완료%`를 붙여 표시합니다.
- 지난 주차에서 끝내지 못한 항목은 지난 주차에 이월 문구를 남기지 않고 다음 주차 목표에 포함합니다.
- 매주 수요일 기준으로 main 반영 상태, PR 상태, 빌드/테스트 결과를 갱신합니다.
- PR은 역할별로 분리하고, main 반영 전 `dotnet build EduStream.sln`과 `dotnet test EduStream.sln --no-build` 결과를 확인합니다.

## 현재 구현 상태

### 1. 공통 코어 / 프로토콜

- Server / Client / Core 프로젝트 분리 ----- %완료%
- 공통 패킷 모델 구성 ----- %완료%
- ACK / Error 응답 모델 구성 ----- %완료%
- 파일 및 화면 패킷 검증 유틸 구성 ----- %완료%
- 패킷 payload 길이 기준 테스트 추가 ----- %완료%
- 직렬화 최적화 후보 검토

### 2. 서버 세션 / 네트워크

- 세션 open / close 흐름 구현 ----- %완료%
- join / leave 기본 흐름 구현 ----- %완료%
- disconnect 처리 흐름 구현 ----- %완료%
- 다중 클라이언트 기준 안정성 검증 ----- %완료%
- heartbeat timeout 정책 정리 ----- %완료%

### 3. 화면 송신

- 화면 캡처 서비스 기반 구성 ----- %완료%
- 화면 송신 루프 기반 구현 ----- %완료%
- 송신 실패 시 로그와 상태 표시 보강 ----- %완료%
- 클라이언트 렌더링 품질 확인

### 4. 파일 전송

- 파일 청크 분할 구현 ----- %완료%
- 수신 조립 구현 ----- %완료%
- 체크섬 검증 구현 ----- %완료%
- metadata mismatch / checksum mismatch 실패 테스트 추가 ----- %완료%
- 수신 진행률과 상태 메시지 모델 보강 ----- %완료%
- UI에서 전송/수신 상태 연결 보강

### 5. 클라이언트 UI / 수신

- 세션 상태 표시 구성 ----- %완료%
- 채팅 상태 표시 구성 ----- %완료%
- 파일/화면 수신 상태 패널 구성 ----- %완료%
- UI 개선 PR 제출 ----- %완료%
- UI PR 빌드 오류 수정 대기
- 시연 흐름 기준 화면 정리

## PPT 기준 월간 로드맵

### 6월 목표: 안정화와 최종 발표 준비

- 역할별 기능 PR을 수집하고 main 반영 상태를 정리
- 다중 클라이언트 기준 검증
- 예외 처리와 실패 메시지 보강
- 파일 전송 UI 흐름 정리
- 화면 공유 품질 확인
- UI 최종 안정화
- 시연 시나리오와 체크리스트 작성
- README / docs / PPT / 최종보고서 내용 동기화

### 7월 목표: 최종 제출물 정리와 발표 품질 보강

- 최종보고서와 발표 자료 내용 일치 확인
- 시연 순서와 담당자별 설명 흐름 확정
- 주요 기능의 실패 상황 대응 문구 정리
- 최종 산출물 백업 및 제출 파일 구성 확인

## 6월 최소 진행 기준

6월은 최종 발표 준비 달이므로 매주 최소 진행 기준을 아래처럼 관리합니다.

### 6월 1주차 최소 기준

- 새 저장소 기준 문서 구조 정리
- 5월 미완료 항목을 6월 기준선으로 재배치
- 역할별 기능 상태와 PR 기준선 확인
- 다중 클라이언트 기준 최소 테스트 진행

### 6월 2주차 최소 기준

- 월요일까지 역할별 PR 또는 작업 브랜치 수집
- 화요일까지 PR 빌드/테스트 확인 및 수정 요청
- 수요일까지 main 반영 상태, PR 상태, 로드맵 업데이트
- 1번/4번 담당 작업 main 반영
- UI PR 빌드 오류 확인 및 수정 요청

### 6월 3주차 최소 기준

- UI 최종 반영 여부 확인
- 세션/채팅/파일/화면 흐름을 하나의 시연 시나리오로 연결
- 다중 클라이언트 기준 시연 리허설 진행
- 실패 메시지와 로그 문구 정리

### 6월 4주차 최소 기준

- 최종보고서 요약본과 발표 자료 내용 동기화
- README / docs / PPT 최종 상태 점검
- 발표 시연 체크리스트 확정
- 제출 전 main 기준 build/test 최종 확인

## 주차별 진행 기준

### 5월 마지막 주

#### 팀 목표치

- 기존 오픈 PR 통합 ----- %완료%
- 새 저장소 생성 및 통합본 push ----- %완료%
- 파일 전송 테스트 보강 ----- %완료%
- 클라이언트 상태 패널 추가 ----- %완료%

#### 담당별 목표치

##### 1. 공통 코어 / 프로토콜

- 공통 패킷 모델 기준 확인 ----- %완료%
- 파일/화면 검증 유틸 기준 확인 ----- %완료%

##### 2. 서버 세션 / 네트워크

- 다중 클라이언트 접속 시 참가자 수와 로그 갱신 확인 ----- %완료%

##### 3. 화면 송신

- 화면 캡처와 송신 루프 기준 확인 ----- %완료%

##### 4. 파일 전송

- chunk 분할, checksum, metadata mismatch 테스트 유지 ----- %완료%

##### 5. 클라이언트 UI / 수신

- 세션/채팅/파일/화면 상태 패널 기준 확인 ----- %완료%

### 6월 1주차

#### 팀 목표치

- 5월 미완료 목표를 6월 기준선으로 이월해 정리 ----- %완료%
- 새 저장소 기준 문서 구조 정리 ----- %완료%
- PPT 로드맵 기준 현재 상태 재정리 ----- %완료%
- 다음 주 작업 기준선 확정 ----- %완료%
- 다중 클라이언트 기준 최소 테스트 진행 ----- %완료%
- 세션/채팅/파일/화면 흐름을 하나의 시연 시나리오로 연결
- 파일 전송 UI 상태 표시 보강
- 화면 공유 수신 상태 확인
- README / docs / PPT 내용 동기화

#### 담당별 목표치

##### 1. 공통 코어 / 프로토콜

- 패킷 모델과 ErrorCode / AckCode가 현재 코드와 맞는지 확인 ----- %완료%
- 파일/화면 검증 유틸이 중복 없이 사용되는지 확인 ----- %완료%
- 시연 중 발생 가능한 ErrorCode / AckCode 부족 여부 확인
- 공통 패킷 검증 규칙과 실제 사용 흐름 비교

##### 2. 서버 세션 / 네트워크

- join / leave / disconnect 흐름 안정성 확인 ----- %완료%
- 교수자 세션 open 이후 수강생 2명 이상 접속 흐름 확인 ----- %완료%
- 비정상 disconnect 이후 참가자 목록 정리 확인

##### 3. 화면 송신

- 화면 캡처와 송신 루프가 시연 가능한 수준인지 확인 ----- %완료%
- 실패 시 로그와 상태 표시가 남는지 확인 ----- %완료%
- 화면 송신 시작/중지 흐름 확인 ----- %완료%
- 클라이언트에서 프레임 수신 상태가 갱신되는지 확인

##### 4. 파일 전송

- UI 연결에 필요한 상태값과 메시지 정리 ----- %완료%
- 실제 파일 선택 후 전송 흐름 확인
- 실패 케이스가 UI 또는 로그에 구분되어 남는지 확인 ----- %완료%

##### 5. 클라이언트 UI / 수신

- 세션/채팅/파일/화면 상태가 한 화면에서 읽히는지 확인
- 수신 실패/대기/완료 상태 표현 방식 정리
- 수강생 기준으로 현재 연결 상태가 명확히 보이는지 확인
- 파일 수신 완료/실패 상태 메시지 정리

### 6월 2주차

#### 팀 목표치

- 역할별 작업 PR 수집 ----- %완료%
- 1번 공통 코어 / 프로토콜 작업 main 반영 ----- %완료%
- 4번 파일 전송 작업 main 반영 ----- %완료%
- 2번 서버 세션 / 네트워크 작업 main 반영 상태 확인 ----- %완료%
- 3번 화면 송신 작업 main 반영 상태 확인 ----- %완료%
- 5번 클라이언트 UI PR 접수 및 빌드 검증 ----- %완료%
- UI PR 빌드 오류 수정 요청 ----- %완료%
- UI 수정본 재검증
- 세션/채팅/파일/화면 흐름을 하나의 시연 시나리오로 연결
- README / docs / PPT / 최종보고서 내용 동기화

#### 담당별 목표치

##### 1. 공통 코어 / 프로토콜

- 패킷 payload 길이 기준을 테스트로 고정 ----- %완료%
- Text/File 패킷 DataLength 기준 검증 ----- %완료%
- Ack/Error 코드 중복 방지 테스트 추가 ----- %완료%
- UI 반영 후 공통 패킷 사용 흐름 재확인

##### 2. 서버 세션 / 네트워크

- heartbeat timeout 정책 main 반영 상태 확인 ----- %완료%
- 다중 클라이언트 세션 테스트 main 반영 상태 확인 ----- %완료%
- UI 병합 후 서버 ViewModel 생성자 호출부 재확인

##### 3. 화면 송신

- 화면 송신 실패 / fallback / 시작 / 중지 로그 보강 main 반영 상태 확인 ----- %완료%
- 프레임 단위 실패 시 송신 루프 유지 흐름 확인 ----- %완료%
- UI 병합 후 화면 송신 상태 표시 확인

##### 4. 파일 전송

- FileReceiveResult 진행률 / 상태 메시지 확장 ----- %완료%
- FileReceiver 수신 대기 / 완료 / 실패 상태 반환 보강 ----- %완료%
- ClientViewModel 파일 수신 상태 표시 연결 ----- %완료%
- UI 병합 후 파일 수신 상태 표시 재확인

##### 5. 클라이언트 UI / 수신

- feature/client-ui PR 제출 ----- %완료%
- PR #6 충돌 상태 clean 확인 ----- %완료%
- PR #6 로컬 build 실패 원인 확인 ----- %완료%
- ServerViewModel 서비스 생성자 호출 수정
- 수정 후 `dotnet build EduStream.sln` 재검증
- 수정 후 `dotnet test EduStream.sln --no-build` 재검증

### 6월 3주차

#### 팀 목표치

- UI PR 수정본 main 반영
- main 기준 전체 build/test 통과 확인
- 교수자 세션 개설부터 수강생 접속, 화면 공유, 파일 전송, 채팅까지 시연 흐름 연결
- 시연 중 실패 상황에 대한 로그와 상태 메시지 확인
- 최종보고서와 발표 자료의 기능 상태 문구 정리

#### 담당별 목표치

##### 1. 공통 코어 / 프로토콜

- UI 병합 이후 공통 패킷 계약 테스트 재확인
- 발표 시 설명할 공통 구조 요약 정리

##### 2. 서버 세션 / 네트워크

- 수강생 2명 이상 접속 시나리오 재검증
- disconnect / heartbeat / 참가자 목록 정리 흐름 재확인

##### 3. 화면 송신

- 화면 공유 시작 / 중지 / fallback 상태를 시연 흐름에서 확인
- 클라이언트 화면 수신 상태와 서버 로그 연결 확인

##### 4. 파일 전송

- 파일 선택 / 전송 / 수신 / 실패 상태를 시연 흐름에서 확인
- 파일 수신 진행률과 완료 메시지 표시 확인

##### 5. 클라이언트 UI / 수신

- 세션 / 채팅 / 파일 / 화면 상태가 한 화면에서 읽히는지 최종 확인
- 발표 시연 기준으로 버튼, 상태 문구, 오류 표시 정리

### 6월 4주차

#### 팀 목표치

- 최종 발표 시나리오 확정
- 최종보고서 요약본과 상세 보고서 문구 정리
- README / docs / PPT 최종 동기화
- main 기준 최종 build/test 결과 기록
- 제출 파일과 백업본 정리

#### 담당별 목표치

##### 1. 공통 코어 / 프로토콜

- 최종 프로토콜 구조 설명 자료 정리

##### 2. 서버 세션 / 네트워크

- 최종 세션 흐름 설명 자료 정리

##### 3. 화면 송신

- 최종 화면 공유 흐름 설명 자료 정리

##### 4. 파일 전송

- 최종 파일 전송 흐름과 검증 결과 설명 자료 정리

##### 5. 클라이언트 UI / 수신

- 최종 UI 화면 캡처와 시연 설명 자료 정리

